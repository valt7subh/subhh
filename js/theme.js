// Theme for terminal - uses black terminal with green text by default
document.addEventListener("DOMContentLoaded", function () {
  // Apply default black terminal theme
  console.log("Terminal theme initialized");
});
